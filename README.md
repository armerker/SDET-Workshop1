Проект Banking Tests
Автотесты для проекта Banking на Python с использованием Selenium WebDriver, pytest и тестирования API.

1. Функциональные возможности
UI‑тесты
 Создание клиента с генерацией данных по спецификации

 Сортировка клиентов по имени

 Удаление клиента по имени средней длины

API‑тесты
 Создание сущности через API

 Получение сущности по ID

 Получение всех сущностей

 Обновление сущности

 Удаление сущности

2. Инфраструктура
📊 Отчёты Allure

🔄 Параллельный запуск тестов

⚙️ Интеграция CI/CD

3. Чек‑лист тестов
3.1. UI‑тесты
3.1.1. Создание клиента (Add Customer)

Генерация 10‑значного почтового индекса

Генерация имени на основе почтового индекса

Проверка успешного создания

3.1.2. Сортировка клиентов по имени (First Name)

Сортировка по алфавиту

Сортировка от Z до A

3.1.3. Удаление клиента

Поиск клиента с длиной имени, близкой к средней

Удаление выбранного клиента

3.2. API‑тесты
Создание сущности — POST /api/create

Получение сущности по идентификатору — GET /api/get/{id}

Получение всех сущностей — POST /api/getAll

Обновление сущности — PATCH /api/patch/{id}

Удаление сущности — DELETE /api/delete/{id}

4. Установка и настройка
4.1. Клонирование репозитория


git clone
cd банковские-тесты
4.2. Создание виртуального окружения

Для Windows:


python -m venv .venv
.venv\Scripts\activate
Для Linux/Mac:


python3 -m venv .venv
source .venv/bin/activate
4.3. Установка зависимостей


pip install --upgrade pip

pip install -r requirements.txt

4.4. Установка Chrome и ChromeDriver
Автоматическая установка (через webdriver‑manager):
При первом запуске тестов ChromeDriver установится автоматически.

4.5. Запуск API‑сервиса (для тестирования API)


# Клонируем и запускаем API‑сервис
git clone ../api-service
cd ../api-service
docker-compose up --build -d

# Возвращаемся в проект с тестами
cd ../banking-ui-tests
5. Запуск тестов

5.1. Запуск всех тестов


pytest all_tests/ -v
5.2. Запуск отдельных категорий тестов

Только UI‑тесты:


pytest all_tests/пользовательский интерфейс/ -v -m пользовательский интерфейс

Только API‑тесты:



pytest all_tests/api/ -v -m api

Конкретные тестовые файлы:


pytest all_tests/ui/test_add_customer.py -v

pytest all_tests/api/test_create_entity.py -v
5.3. Параллельный запуск


pytest all_tests/ -n auto --tb=short
5.4. Запуск по маркерам


pytest all_tests/ -m smoke -v

pytest all_tests/ -m regression -v

pytest all_tests/ -m api -v

pytest all_tests/ -m ui -v

6. Генерация Allure‑отчётов

6.1. Генерация результатов

bash
# Запуск тестов с генерацией Allure‑результатов
pytest all_tests/ -v --alluredir=allure-результаты

6.2. Генерация и просмотр отчёта

Генерация статического отчёта:

bash
allure generate allure-results -o allure-report --clean

Локальный просмотр:

bash
# После запуска тестов с Allure
allure serve allure-results
Откроется браузер с подробным отчётом: http://localhost:8080

6.3. Структура отчёта

Обзор — общая статистика по тестам

Suites — группировка тестов по сьютам

Графики — диаграммы и метрики

Временная шкала — график выполнения

7. Интеграция CI/CD
7.1. GitHub Actions

При каждом пуше в ветку main/master автоматически запускаются тесты и генерируется отчёт Allure.

7.2. Просмотр отчёта в CI
После завершения пайплайна CI:

Перейдите в раздел «Действия» в вашем репозитории.

Выберите последний запуск workflow.

В разделе «Артефакты» скачайте allure-report.

Распакуйте архив и откройте index.html в браузере.

После настройки отчёт будет доступен по адресу:
https://.github.io//

8. Структура проекта

```bash

banking-ui-tests/
├── all_tests/                    # Все тестовые файлы
│   ├── ui/                       # UI тесты
│   │   ├── test_add_customer.py     # Тесты создания клиента
│   │   ├── test_customer_sorting.py # Тесты сортировки
│   │   └── test_delete_customer.py  # Тесты удаления
│   └── api/                      # API тесты
│       ├── test_create_entity.py    # Создание сущности
│       ├── test_get_entity.py       # Получение сущности по ID
│       ├── test_get_all_entities.py # Получение всех сущностей
│       ├── test_update_entity.py    # Обновление сущности
│       └── test_delete_entity.py    # Удаление сущности
├── pages/                       # Page Object модели
│   ├── base_page.py            # Базовый класс страницы
│   ├── manager_page.py         # Страница менеджера
│   └── customers_page.py       # Страница клиентов
├── utils/                       # Вспомогательные утилиты
│   └── test_data_generator.py  # Генератор тестовых данных
├── data.py                     # Тестовые данные и конфигурация
├── conftest.py                 # Фикстуры pytest
├── pytest.ini                  # Конфигурация pytest
├── requirements.txt            # Зависимости проекта
└── README.md                   # Документация